# Source provenance — Hue Scene Recall v0.3.4 release kit

- Verified live/HACS repository: `psuslick/hue-scene-recall`
- Verified installed Git revision: `d60dcee`
- Verified installed manifest: `0.3.3`
- Installed source was inspected read-only through the Home Assistant File editor/HACS interfaces.
- The patch script is deliberately strict and refuses a different Git revision when `.git` is present. It also validates exact v0.3.3 manager source markers before applying anything.
- This environment could not clone GitHub directly, so the durable deliverable is a deterministic source patch/release kit, not a falsely reconstructed full repository archive.
- No live Home Assistant file, Hue Scene, Smart Scene, bulb state, or HACS installation was modified while building this kit.
